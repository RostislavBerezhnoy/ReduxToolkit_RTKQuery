# ReduxToolkit with RTK Query

Для запуска фейкового бэкенда потребуется [JSON Server](https://github.com/typicode/json-server).

**Установка пакета:**
> npm install -g json-server

**Запуск сервера:**
> json-server --watch db.json --port 5000
